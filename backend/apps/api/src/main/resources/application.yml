spring:
  application:
    name: cleat-api

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    defaults:
      enabled: true
    redis:
      enabled: false
    db:
      enabled: true